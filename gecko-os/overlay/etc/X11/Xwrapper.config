# Allow non-root users to start Xorg via xinit. Without this Debian's
# Xorg.wrap blocks any user other than the active console session.
# Gecko OS only ever has the `gecko` user starting X — there's nothing
# to escalate to.
allowed_users=anybody
needs_root_rights=yes
