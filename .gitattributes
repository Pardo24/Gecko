# Force LF line endings for shell scripts and other files that run inside
# Linux containers — Windows CRLF would break sh interpreters and Python.
*.sh        text eol=lf
*.py        text eol=lf
Dockerfile  text eol=lf
